# Margin = figure's short side * ratio
ratio = 0.05
# Clipboard polling interval, in seconds
poll_interval = 0.3
# Background match tolerance (max per-channel RGB difference, 0-255)
tolerance = 20
# Side length, in px, of the corner sampling boxes
corner_size = 8
